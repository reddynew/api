api
const response = await axios.post('api for local host',data) const data={name:name,email:email}

from fastapi import fastapi
app=fastapi()

// python3 -m uvicorn app:app --reload
uvicorn-- server
app-- name of the file 
-- return jsonify(df.to_dict(orient='records'))